[project]
name = "roguelike"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncio>=4.0.0",
    "pydantic>=2.13.1",
    "pyxel>=2.9.0",
    "websockets>=16.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]
